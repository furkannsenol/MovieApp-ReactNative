import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const viewAllMovieItem = () => {
  return (
    <View>
      <Text>viewAllMovieItem</Text>
    </View>
  )
}

export default viewAllMovieItem

const styles = StyleSheet.create({})